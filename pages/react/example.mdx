import { MDXProvider } from "@mdx-js/react";

export const variables = {
  mojaZmienna: "To jest zawartość mojej zmiennej!",
};

export const MyMdxComponent = () => {
  return (
    <MDXProvider components={variables}>
      ``` {variables.mojaZmienna} ```
    </MDXProvider>
  );
};

<MyMdxComponent />

export const Foo = () => {
  return <div>Foo</div>;
};

# Another Page zzzxxx

```jsx filename="demo.js" copy enableInterpolation
{
  Foo.toString();
}
```

## Component

import { useState } from "react";

{/* Import CSS modules */}
import styles from '../../components/counters.module.css'

export const Counter = () => {
  const [count, setCount] = useState(0);
  return (
    <div>
      <button onClick={() => setCount(count + 1)} className={styles.counter}>
        Clicked {count} times
      </button>
    </div>
  );
};

<Counter />

## External Component

import Counters from "../../components/counters";

<Counters />

export const Thing = () => <>World</>;

# Hello <Thing />

import { useEffect, useRef } from "react";

export const Parser = ({ children, component }) => {
   const ref = useRef();
   const tokenRef = useRef();
    
  useEffect(() => {
   if (ref.current) {
     const token = [...ref.current.querySelectorAll("code span")].find(
       (el) => el.innerText === "{myComponent}"
      );
     tokenRef.current = token;
    }
}, []);

useEffect(() => {
if (tokenRef.current) {
tokenRef.current.innerText = Thing.toString();
}
}, []);

console.log(Thing.toString());

return (

<div ref={ref}>{children}</div>
); };

<Parser component={Thing()}>
  ```js filename="dynamic_code.js"
  {myComponent}
  ```
</Parser>
